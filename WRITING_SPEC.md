# 《从 If 到 LLM》写作规范

> 这份文件是全书唯一的风格与结构约束。
> 写任何一章之前先读完它。它比你的直觉重要。

---

## 0. 这本书到底在干什么

不是"Transformer 原理教程"。

**这本书要让读者一次次遇到问题，最终不得不自己把 Transformer 发明出来。**

读者跟着走完 30 章之后，应该产生这样的感觉：

> "哦，原来 Attention 是这么被逼出来的。"
> "哦，原来这就是我第 4 章写过的那个东西，只是长大了。"
> "哦，原来 GPT 里没有一个模块是凭空出现的。"

而**不是**：

> "记住了，Transformer 有 6 个组件。"

### 目标读者

组内工程师。会写 Python，会用命令行，**没有系统学过机器学习**。
他们听过梯度、听说过 Attention、用过 ChatGPT，但不知道这些东西是怎么被造出来的。

所以：

- 不要假设读者懂微积分。**所有数学都必须先有直觉，再有公式。**
- 不要假设读者懂机器学习术语。**任何术语都必须先被发明，再被命名。**
- 不要假设读者的耐心是无限的。**每章必须能在 20 分钟内读完并跑通。**

---

## 1. 七条铁律

1. **每章必须从上一章留下的问题开始。** 第一章不能提前解决第二章的问题。
2. **每章只引入一个主要新概念。** 多的概念要么删掉，要么留给后面。
3. **不允许提前使用尚未解释的概念。** 出现的每个名词，读者都必须已经见过它的动机。
4. **新机制必须由上一章的缺陷自然引出。** 先让读者亲眼看到旧方法失败，再给出新方法。
5. **每章代码必须可以独立运行。** 不允许"必须先跑第 3 章才能跑第 7 章"。
6. **第 1–8 章只用 Python 标准库。** 第 9 章起可以用 numpy 和 `toygrad`。第 25 章起可以用 PyTorch。
7. **代码优先可读性，不追求性能。** 变量名写全、循环写慢一点没关系，看不懂才是问题。

### 命名纪律（最容易违反的一条）

一个概念在**被命名之前**，只能用它"做什么"来描述。

| 章节 | 可以这样写 | 还不能这样写 |
|---|---|---|
| 第 2 章 | "一堆 if/else 规则" | "分类器"、"机器学习" |
| 第 3 章 | "把规则变成数字"、"分数" | "线性分类器"、"权重向量" |
| 第 7 章 | "中间加一层" | "隐藏层"、"多层感知机"（命名可以做，但必须先做完实验） |

**先做实验，再命名。** 命名那一刻要明确写出来："这个东西，以后我们叫它 X。"

---

## 2. 每章固定目录

```
chapter_XX/
├── README.md          # 正文，这一章的主体
├── before.py          # 上一章的代码（或它在这章场景下的直接应用）。跑起来会失败/不够好
├── after.py           # 本章的产物。跑起来能看到改善
├── experiment.py      # 让读者亲眼看到"旧方法为什么不行 / 新方法为什么行"
├── exercises.md       # 练习
└── tests/
    └── test_chapter_XX.py
```

`before.py` 的含义：**把这章的问题摆到台面上。**
它通常是上一章 `after.py` 的副本，或者是上一章的思路用在本章数据上——然后暴露缺陷。

顶部的路径引导（所有用到 toygrad 或需要 import 兄弟文件的章节都要写）：

```python
import os
import sys

sys.path.insert(0, os.path.join(os.path.dirname(os.path.abspath(__file__)), ".."))

from toygrad import Tensor
```

---

## 3. README.md 固定结构

**每一章都必须严格按这八个小节写，标题一字不改：**

```markdown
# 第 X 章：<标题>

## 本章的问题

上一章哪里不够？把一个具体的、看得见的现象摆在读者面前。
不要抽象地说"表达能力不足"，要说"我们跑了一下，它把这句话分错了"。

## 最简单的尝试

先用最直觉的方法解决。哪怕它是错的。
这一节要让读者觉得"我也能想到这个办法"。

## 实验

跑 `experiment.py`，贴出真实输出。
让读者亲眼看到旧方法在哪里失败。
**这一节的输出必须是真实运行得到的，不许编。**

## 新机制

只介绍本章真正需要的那一个新概念。
先讲它为什么能解决刚才的失败，再给它名字。
公式和 Python 实现必须成对出现。

## Python 实现

完整代码讲解，对应 `after.py`。
关键行要有解释，但不要逐行注释。

## 它解决了什么

明确说清楚这一章带来了什么能力。用实验数据说话。

## 它还解决不了什么

自然引出下一章。
这一节是全书最重要的一节——它是链子的扣环。
结尾必须是一个具体的问题，不是一个笼统的方向。

## 练习

见 `exercises.md`，这里列 3–5 条最关键的。
```

### 写作语气

- 用"我们"，不用"笔者"。
- 允许口语化。允许说"这看起来很蠢，但先忍一下"。
- 不吹嘘，不煽情。**让实验数据说话**，不要写"这真是太神奇了"。
- 每章开头可以直接承接上一章的结尾，例如："上一章最后留下了一个问题：……"

### 禁止事项

- ❌ 在 README 里出现"正如你所知"、"众所周知"、"简单来说"（后面往往跟着不简单的东西）
- ❌ 贴一章代码然后说"代码如下"
- ❌ 用未定义的符号
- ❌ 编造实验输出
- ❌ 写"这为后续章节打下基础"这种空话

---

## 4. 代码规范

- Python 3.12。文件顶部不加 shebang。
- 注释、print 输出、变量名解释**全部用中文**；变量名本身用英文。
- 每个 `.py` 文件开头有一段 docstring 说明"这个文件演示了什么"。
- `print` 的输出要**可读**：带标题、带分隔线，让读者一眼看到关键对比。

```python
print("=" * 60)
print("旧方法：")
print("=" * 60)
```

- 随机性必须固定种子，保证读者跑出来和书上一样。
- 不要写超过 3 层的嵌套。
- 不要用 `try/except` 掩盖错误——让错误暴露出来，那是教学素材。

### 运行时

虚拟环境在仓库根目录：

```bash
./.venv/bin/python chapter_03/after.py
./.venv/bin/python -m pytest chapter_03/tests/ -q
```

---

## 5. toygrad 接口（第 9 章起可用）

第 9 章的产物，位于仓库根目录 `toygrad/`。

```python
from toygrad import Tensor, cross_entropy, embedding, layer_norm, masked_fill, no_grad, randn, zeros
from toygrad import SGD, Adam
```

### Tensor

| 接口 | 说明 |
|---|---|
| `Tensor(data, requires_grad=False)` | `data` 是 numpy 数组或标量 |
| `.data` / `.grad` | 数值 / 梯度（numpy 数组） |
| `.shape` `.ndim` `.size` `.T` | 形状相关 |
| `+ - * / **` | 逐元素，支持 numpy 式广播 |
| `@` | 矩阵乘法，支持批量维广播 |
| `.exp() .log() .relu() .tanh() .gelu() .maximum(v)` | 逐元素函数 |
| `.reshape(...) .transpose(...)` / `x[切片或整数数组]` | 形状变换 |
| `.sum(axis, keepdims) .mean(axis, keepdims)` | 归约 |
| `.softmax(axis=-1)` | 沿最后一维归一化 |
| `.masked_fill(mask, value)` | mask 为 True 处填 value |
| `.backward()` | 反向传播，梯度**累加**到 `.grad` |
| `.zero_grad()` | 清空梯度 |
| `.item()` | 取出标量值 |

### 函数

```python
cross_entropy(logits, targets)          # logits: (..., V)，targets: (...) 整数
layer_norm(x, weight, bias, eps=1e-5)   # 归一化最后一维
embedding(table, idx)                   # 查表：(V, D)[idx] -> idx.shape + (D,)
randn(*shape, scale=1.0, requires_grad=False, seed=None)
zeros(*shape, requires_grad=False)
no_grad()                               # 上下文管理器，块内不记录计算图
```

**重要**：`backward()` 是**累加**梯度的。每轮训练必须 `zero_grad()`。这是刻意设计成和 PyTorch 一样的，要在第 9 章讲清楚。

### SGD / Adam

```python
opt = SGD(model_params, lr=0.1)
opt.zero_grad()
loss.backward()
opt.step()
```

---

## 6. 测试写法

`tests/test_chapter_XX.py`，用 pytest，但也要能直接 `python` 运行：

```python
import os
import sys

sys.path.insert(0, os.path.join(os.path.dirname(os.path.abspath(__file__)), ".."))

from after import ...   # 从本章 after.py 导入


def test_xxx():
    ...


if __name__ == "__main__":
    import pytest
    sys.exit(pytest.main([__file__, "-v"]))
```

测试要测**这一章真正的新机制**，不要测无关紧要的东西。
每个测试的命名要说人话，例如 `test_attention_weights_sum_to_one`。

---

## 7. 全书大纲

每一章的标题、它要解决的问题、它留下的问题，**已经定死，不许改**。

### 第一部分：机器为什么能"理解"文字

| 章 | 标题 | 解决的问题 | 留下的问题 |
|---|---|---|---|
| 1 | 先把一句话拆开 | 计算机拿到"苹果发布了新手机"，看到的到底是什么 | 词拆出来了，怎么知道一句话属于什么类别？ |
| 2 | 用 if/else 教机器分类 | 用一堆 if/elif 给句子分类 | 几万个词，难道写几万个 if？ |
| 3 | 把 if 变成数字 | 把 `if "手机" in words` 改写成加权求和的分数 | 这些 1.2、0.8、1.5 谁来决定？ |
| 4 | 让程序自己修改权重 | 分类错了就调整权重——第一次"学习" | 每次到底应该改多少？ |
| 5 | 错误到底有多大 | 用一个数字衡量"错得有多离谱"（loss） | 参数很多以后，一个个试着改太慢了 |
| 6 | 机器怎么知道参数往哪边改 | 数值法求梯度 → `w = w - lr * grad` | 每一层都要手写梯度，层多了会写死人 |

### 第二部分：从分类器到神经网络

| 章 | 标题 | 解决的问题 | 留下的问题 |
|---|---|---|---|
| 7 | 一条直线不够用了 | XOR 分不开，必须加一个中间层 | 中间层加了，但它没用 |
| 8 | 为什么需要激活函数 | 证明线性叠线性还是线性，引入 ReLU | 每层梯度都要手推，越写越难 |
| 9 | 让机器自己算梯度 | 实现 `Tensor` 自动求导引擎 | 词还只是编号，编号没有意义 |

### 第三部分：文字如何变成"意义"

| 章 | 标题 | 解决的问题 | 留下的问题 |
|---|---|---|---|
| 10 | 数字编号没有意义 | 一个词应该是一组数字（embedding） | 这组数字怎么来？随机的吗？ |
| 11 | 让相似的词靠近 | 用上下文训练 embedding，相似词自然聚拢 | 一个词只能有一种意思 |
| 12 | 一个词不能只有一种意思 | 用上下文向量表示词：`context = 周围词的平均` | 平均太粗暴了，不是每个词都一样重要 |

### 第四部分：Attention 为什么出现

| 章 | 标题 | 解决的问题 | 留下的问题 |
|---|---|---|---|
| 13 | 不是每个词都一样重要 | 按相似度加权求和（最原始的 Attention） | 它只知道"像不像"，不知道"我在找什么" |
| 14 | 我找什么、你有什么 | 拆出 Q / K / V，实现单头 self-attention | 一种"看的方式"不够 |
| 15 | 为什么需要多个头 | 多头注意力：并行多种关系 | 顺序信息丢了 |
| 16 | 模型不知道谁在前谁在后 | 位置编码 | 层数一多就学不动 |

### 第五部分：Transformer 是怎么被"逼"出来的

| 章 | 标题 | 解决的问题 | 留下的问题 |
|---|---|---|---|
| 17 | 层数多了反而学不会 | 残差连接 `x = x + layer(x)` | 数值尺度还是乱 |
| 18 | 数据越传越乱 | LayerNorm | Attention 只会交换信息，不会加工信息 |
| 19 | Attention 后面为什么还有 MLP | 组装出完整的 Transformer Block | 输出还是向量，不是词 |

### 第六部分：从 Transformer 到语言模型

| 章 | 标题 | 解决的问题 | 留下的问题 |
|---|---|---|---|
| 20 | 把分类器重新拿回来 | 下一 token 预测 = 分类，闭环回到第 2 章 | 训练时偷看了答案 |
| 21 | 为什么训练时不能偷看答案 | 因果掩码 | 该把所有零件装起来了 |
| 22 | 第一个真正的小语言模型 | 组装 + 训练一个字符级小 GPT | 它只会预测，不会生成 |
| 23 | 模型为什么会"生成" | 自回归生成、temperature、top-k | 它到底"理解"了什么？ |
| 24 | 它为什么看起来像在思考 | 观察（**本章不加新机制，也不加代码机制**） | 玩具模型为什么这么弱 |

> 第 24 章特殊：它是纯观察章。`after.py` 可以是"用第 22 章的模型做一组探测实验"，
> 不做任何架构改动。目的是让读者自己观察模型拥有了什么、缺了什么。
> 结尾引出：规模。

### 第七部分：把玩具模型变成现代 LLM

| 章 | 标题 | 解决的问题 | 留下的问题 |
|---|---|---|---|
| 25 | 用 PyTorch 重写我们的模型 | 逐个替换成 `torch.Tensor` / `nn.Linear` / `nn.Embedding` | 我们的分词太粗糙 |
| 26 | 真正的 tokenizer | 实现 mini-BPE | 我们和 GPT 还有什么差别？ |
| 27 | GPT | 逐项对照，发现没有神奇模块 | 为什么它这么大？ |
| 28 | 为什么模型越大越厉害 | 参数/数据/算力与损失的关系（scaling law） | 会预测下一个词，不等于会聊天 |
| 29 | 从语言模型到 ChatGPT | 预训练 → 指令微调 → 偏好对齐 | — |
| 30 | 回到第一行代码 | 首尾呼应，全书压缩成一张演化图 | 完 |

> 第 28、29 章没有"新机制"可写，属于**观察与讨论章**。
> 它们仍按八节模板写，但 `experiment.py` 可以是数据分析实验
> （例如：用不同参数量训练同一个模型，画 loss 对比表）。
> 不要求跑真 GPT。

---

## 8. 贯穿全书的统一例子（务必沿用，不要自创）

全书用**同一批句子**从头走到尾，这样读者在第 20 章还能认出第 1 章的那句话。

### 第 1–6 章：短句分类

语料固定为：

```python
科技: 苹果发布新手机 / 苹果发布新芯片 / 华为发布新电脑 / 小米发布新手机 / 苹果芯片很强
食品: 苹果很好吃 / 苹果很甜 / 香蕉很好吃 / 这个苹果真甜 / 苹果做成派
```

词表固定为：

```text
苹果 发布 新 手机 芯片 电脑 华为 小米 好吃 很 甜 香蕉 这个 真 做成 派
```

注意"苹果"同时出现在两个类别里——这是刻意的，为第 12 章"一个词不能只有一种意思"埋下伏笔。

### 第 10–12 章：词向量

语料扩展到同一批词的共现统计，"苹果"要靠**上下文**才能确定是水果还是公司。

### 第 13–16 章：长句

固定用这两句：

```text
我 昨天 在 商场 看到 苹果 刚刚 发布 的 新 手机
小王 把 书 给了 小李 因为 他 明天 考试
```

第二句是第 15 章讲"多头"的关键例子（指代关系 / 因果关系 / 时间关系）。

### 第 16 章：词序

```text
狗咬人
人咬狗
```

### 第 20–24 章：字符级语言模型

语料固定为一小段中文古诗 + 儿歌（自造或公有领域，**不要用有版权的文本**）：

```text
床前明月光疑是地上霜举头望明月低头思故乡
```

外加几句补充语料，保证字符种类在 30 个左右，小到可以在 CPU 上几分钟训练完。

### 术语首次出现的章节（不许提前）

| 术语 | 首次出现 |
|---|---|
| token / 词表 / token id | 第 1 章 |
| 特征、分数（score） | 第 3 章 |
| 权重（weight）、学习（训练） | 第 4 章 |
| 损失（loss）、概率、softmax | 第 5 章 |
| 梯度、学习率 | 第 6 章 |
| 隐藏层、神经网络 | 第 7 章 |
| 激活函数、非线性 | 第 8 章 |
| 自动求导（autograd） | 第 9 章 |
| 词向量 / embedding | 第 10 章 |
| 上下文 | 第 12 章 |
| Attention | 第 13 章 |
| Query / Key / Value | 第 14 章 |
| 多头 | 第 15 章 |
| 位置编码 | 第 16 章 |
| 残差连接 | 第 17 章 |
| LayerNorm / 归一化 | 第 18 章 |
| Transformer Block | 第 19 章 |
| 语言模型 | 第 20 章 |
| 因果掩码 | 第 21 章 |
| 自回归、temperature、top-k | 第 23 章 |
| tokenizer（作为"把文本变成编号"的通称） | 第 20 章 |
| BPE、byte-level BPE | 第 26 章 |

> 关于 tokenizer：第 1 章**刻意不给它起名字**（那时读者只该理解"把句子拆开"），
> 但到第 20 章之后，"tokenizer"作为"把文本变成编号的那一步"的通称可以使用，
> 因为第 22 章要组装整条流水线，没有名字反而别扭。
> 第 26 章讲的是**真正的 tokenizer 是怎么设计的**（词表爆炸、未登录词、BPE），
> 那才是这个词被真正解释的地方。

---

## 9. 完稿前自检

写完之后，逐条确认：

- [ ] `./.venv/bin/python chapter_XX/after.py` 能跑通，输出有意义
- [ ] `./.venv/bin/python chapter_XX/before.py` 能跑通，并且**确实暴露了本章要说的问题**
- [ ] `./.venv/bin/python chapter_XX/experiment.py` 能跑通
- [ ] `./.venv/bin/python -m pytest chapter_XX/tests/ -q` 全绿
- [ ] README 的八个小节标题一字不差
- [ ] README 里贴的实验输出是真的（从终端复制的）
- [ ] 本章没有出现尚未解释的概念
- [ ] "它还解决不了什么"结尾是一个**具体问题**
- [ ] 所有随机过程都固定了种子

**没跑通就是没写完。** 不要提交跑不通的章节。

### 关于脚本耗时

单个脚本**尽量**控制在 1 分钟内。但有两处例外是刻意保留的，不要为了压时间砍掉实验完整性：

- `chapter_22/experiment.py`（~1.5 分钟）：逐零件消融，每个都要重训一次。
- `chapter_24/experiment.py`（~3 分钟）：训 7 个模型做 2×2 交叉评估，这是那一章唯一的证据来源。

这两处的耗时已经写在根目录 `README.md` 里提醒读者了。
